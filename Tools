# Install git repository into conda
## Activate your conda environment source
activate myenv
# install git and pip modules
conda install git pip
# install packages.
pip install git+git://github.com/scrappy/scrappy@master
